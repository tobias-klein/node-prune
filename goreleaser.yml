build:
  main: cmd/node-prune/main.go
  binary: node-prune
  goos:
    - darwin
    - linux
    - windows
  goarch:
    - amd64
changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^refactor'
